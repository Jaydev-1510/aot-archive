import type { SeedDataset } from "../../types";

export const dataset: SeedDataset = {
  // All relationships are organized in domain-specific files:
  // family.ts, faction.ts, location.ts, historical.ts
};
